# ── Core (always required) ────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
networkx>=3.0
reportlab>=4.0.0
playwright>=1.40.0

# ── Tree-sitter AST extraction ────────────────────────────────────────────────
# Core parser
tree-sitter>=0.21.0

# Language bindings — install only the ones your repos use
tree-sitter-python>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0
tree-sitter-go>=0.21.0
tree-sitter-rust>=0.21.0
tree-sitter-java>=0.21.0
tree-sitter-c>=0.21.0
tree-sitter-cpp>=0.21.0
tree-sitter-c-sharp>=0.21.0
tree-sitter-ruby>=0.21.0
tree-sitter-kotlin>=0.21.0

# ── Optional: community detection quality upgrade ─────────────────────────────
# Falls back to networkx Louvain if not installed
graspologic>=3.0.0   # enables Leiden algorithm (better than Louvain)

# ── Optional: AWS Bedrock ─────────────────────────────────────────────────────
boto3>=1.26.0
langchain-aws>=0.2.0

# ── Optional: Google Gemini ───────────────────────────────────────────────────
# langchain-google-genai>=2.0.0
